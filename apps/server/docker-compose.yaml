services:
  #  app:
  #    build:
  #      context: .
  #      no_cache: true
  #    container_name: payment-gateway-be
  #    env_file:
  #      - .env
  #    ports:
  #      - "8085:8085"
  #    depends_on:
  #      - postgres
  #    restart: unless-stopped
  postgres:
    image: postgres:15
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: payment_gateway
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: